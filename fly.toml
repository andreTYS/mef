app            = "mef-transparencia"
primary_region = "lim"

[build]

[env]
  PORT     = "3000"
  NODE_ENV = "production"
  PLAYWRIGHT_BROWSERS_PATH = "/ms-playwright"

[http_service]
  internal_port        = 3000
  force_https          = true
  auto_stop_machines   = false
  auto_start_machines  = true
  min_machines_running = 1

  [http_service.concurrency]
    type       = "requests"
    soft_limit = 25
    hard_limit = 50

  [[http_service.checks]]
    path         = "/api/status"
    interval     = "30s"
    timeout      = "15s"
    grace_period = "30s"

[[vm]]
  memory   = "1gb"
  cpu_kind = "shared"
  cpus     = 1

[[mounts]]
  source      = "mef_data"
  destination = "/app/data"
